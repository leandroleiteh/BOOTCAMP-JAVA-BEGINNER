package edu;

public class ControleDeFluxos {

	public static void main(String[] args) {
		
		//ESTRUTURAS DE DECISÃO: Estrutura que avalia uma condição booleana ou variável para direcionar o fluxo de execução
		//OPÇÕES: if(se), switch(escolha), e operador ternário (?, : )

		
		
		
		//TIPO: DECISÃO (if, if-else, if-else-if)
		
		/*
		if (condição){                 if (condição){                 if (condição) {
		}                              } else {                       } else if (condição){ 
		                               }                              } else{
		*/
		
		
		
	                                                       
		
		//TIPO: ESTRUTURA LINEAR DECISÃO OPERADORES TERNÁRIOS ( ?, : )      *EVITAR USO
		
	    /*
	     
	     condição ? true : false;             condição ? true : null;
	     
	     ligado ? desligar : ligar;           emMovimento ? freia : null;
	
	    */
		
		
		
		
		//TIPO: DECISÃO (switch)
		
		/*
		
		VARIÀVEL:                              switch (variavel) { 
		byte                                    case "AZUIS" :
		short                                    break;
		char                                    case "VERDES" : 
		int                                      break;
		Enum                                    default:
		String                                   break;
		                                       }
		
		*/
		
		
		
		
	}


}